# HR Agent App

LangGraph-приложение, собранное из `hr_agent.ipynb`.

## Запуск CLI

```bash
python -m hr_agent_app.cli
```

## Запуск LangGraph dev-сервера

```bash
langgraph dev
```

Переменные окружения берутся из `.env`. Минимально нужен `OPENROUTER_API_KEY`.
